# Test environment variables (safe placeholders)
# These are stub values used for local and CI tests. Do NOT use real secrets.

# Supabase (stubbed; tests intercept network calls)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:31337
NEXT_PUBLIC_SUPABASE_ANON_KEY=stub-anon-key

# Auth bypass toggle used in some pages (keep false for auth tests)
NEXT_PUBLIC_BYPASS_AUTH=false

# Analytics / SEO (stubs)
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-TESTING123
NEXT_PUBLIC_GOOGLE_SEARCH_CONSOLE_VERIFICATION=TEST-VERIFICATION-CODE

# Weather API (not used in tests; present for completeness)
NEXT_PUBLIC_WEATHER_API_KEY=stub-weather-key
